/**
 * typed_parser — a parser generator that runs entirely in the TypeScript type system.
 *
 * This library is type-only: the public surface is types, not runtime values.
 * Implementation has not started yet — see README.md for the design and CLAUDE.md
 * for the bottom-up build order (layer 0: type-level utilities).
 */

export {};
